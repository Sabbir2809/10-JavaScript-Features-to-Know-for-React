/*
  Topic: Ternary Operator
  1. Main usage in React for conditional rendering within JSX.
  Syntax: condition ? TrueCondition : FalseCondition
*/

const loggedIn = false;

const userName = loggedIn ? 'Sabbir' : 'Guest';
console.log(userName);
